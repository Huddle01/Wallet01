{
  "name": "@wallet01/react",
  "version": "1.0.0",
  "main": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist/",
    "build": "yarn clean && tsup --format esm --dts"
  },
  "dependencies": {
    "@wallet01/multichain": "*",
    "@tanstack/react-query": "^4.14.5",
    "jotai": "^1.9.1",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.25",
    "rimraf": "^3.0.2",
    "tsup": "^6.4.0"
  }
}
